# NMS-Project Documentation

This repository contains the RCA Platform (Network Management System) dashboard and supporting scripts.

- `src/` - Python source code for the platform (dashboard, processors, generators).
- `docs/` - Documentation (this folder).
- `scripts/` - Helper scripts to run and install dependencies.

See `usage` for quick start instructions.
